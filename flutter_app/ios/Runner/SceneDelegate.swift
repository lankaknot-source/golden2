import Flutter
import UIKit

// The scene class is referenced by name from Info.plist. Explicitly exposing
// the Objective-C name keeps it loadable in release archives as well as debug
// builds (otherwise iOS can leave the app on a blank scene).
@objc(SceneDelegate)
class SceneDelegate: FlutterSceneDelegate {

}
